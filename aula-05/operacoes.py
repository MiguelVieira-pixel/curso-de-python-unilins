def soma(a,b): # soma
    return a + b

def subtracao(a,b): #subtração
    return a - b

def multiplicacao(a,b): #multiplicação
    return a * b

def divisao(a,b): # divisão
    if b != 0:
        return a / b
    else:
        return 'Erro: Divisão por zero não é permitido'